node_modules/
data/
.env
dist/
*.db
*.enc
*.tsbuildinfo
.DS_Store
.claude/
progress-*.txt

# searxng/ is NOT ignored — it contains config, not data

# ralphex scratch output (progress logs, run summaries)
.ralphex/
.superpowers/

# Claude Code local settings (auto-updated by IDE, not meaningful for R2)
.claude/settings.json

# Tailscale-issued HTTPS certs for dev (never commit private keys)
.tailnet-cert/

# Stray pnpm lockfile — this repo uses npm workspaces (package-lock.json)
pnpm-lock.yaml
